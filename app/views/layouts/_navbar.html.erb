<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= products_path%>">Shopping Cart</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="<%= orders_path %>">Carrito de compras</a></li>
      </ul>
      <ul class="nav navbar-nav">
        <li><a href="<%= payed_orders_path%>">Ordenes de compra</a></li>
      </ul>
      <%if user_signed_in?%>
      <ul class="nav navbar-nav">
        <li><a href="<%=admin_root_path%>">Admin</a><li>
      </ul>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in?%>
        <li><a href="#">Bienvenido(a): <%= current_user.email %></a></li>
      		<% else %>
         <li><a href="<%=new_user_session_path%>">Ingreso</a></li>
          <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" 
          aria-haspopup="true" aria-expanded="false"><span class="caret"></span>Menu</a>
           
          <ul class="dropdown-menu">
            <%if user_signed_in? %>           
            <li><a href="<%=edit_user_registration_path%>">Actualizar datos </a></li>
            <li><a href="<%=destroy_user_session_path%>" data-method='DELETE'>Salir</a>
            </li>
            <%else%>
            <li><a href="<%=new_user_session_path%>">Ingreso</a></li>
            <li><a href="<%=new_user_registration_path%>">Registro</a></li>
            <%end%>
          </ul>
        </li>
      </ul>

	
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
